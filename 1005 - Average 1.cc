#include <bits/stdc++.h>
using namespace std;

int main() {
    double A, B;
    cin >> A >> B;
    
    double X = (A * 3.5 + B * 7.5) / 11.0;
    
    
    cout << fixed << setprecision(5) << "MEDIA = " << X << endl;
    
    return 0;
}
